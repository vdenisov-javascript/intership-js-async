const assert = require('assert');


describe('Testing "task-5" from "Javacript-Async"', () => {

  it('I can`t imagine test for it', () => {
    const myResults = { reason: 'because "tail" just print to console' };

    assert.deepEqual(true, true);
  });

});
